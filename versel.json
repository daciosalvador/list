{
    "builds": [{
        "src": "django_todo_app/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "django_todo_app/wsgi.py"
        }
    ]
}